class IndexController < ApplicationController

  def show_index
    @params = params
  end

  def show
    index_name = params[name:, description:]
    index = {name: "Flashcards", description: "learning tool"}
    x = index[]
  end

end
